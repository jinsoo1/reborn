<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.reborn.reborn">

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.INSTALL_SHORTCUT"/>


    <application
        android:name=".base.App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Reborn"
        tools:targetApi="31">

        <activity android:name=".ui.view.start.StartActivity"
            android:exported="true"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />

            </intent-filter>

        </activity>

        <activity android:name=".ui.view.login.LoginActivity"/>
        <activity android:name=".ui.view.main.MainActivity"/>

        <activity
            android:name="com.kakao.sdk.auth.AuthCodeHandlerActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <!-- Redirect URI: "kakao${NATIVE_APP_KEY}://oauth" -->
                <data android:host="oauth"
                    android:scheme="kakaob5cce02d691dc3b8070098c2dbece43e" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.view.account.AccountActivity"/>
        <activity android:name=".ui.view.assessment.AssessmentActivity"/>
        <activity android:name=".ui.view.assessment.analysis.ProgressActivity"/>
        <activity android:name=".ui.view.main.search.SearchResultActivity"/>
        <activity android:name=".ui.view.myroutine.MyRoutineActivity"/>
        <activity android:name=".ui.view.main.search.related.exercise.ExerciseActivity"/>
        <activity android:name=".ui.view.main.search.related.disease.DiseaseActivity"/>
        <activity android:name=".ui.view.recommend.RecommendActivity"/>
    </application>

</manifest>