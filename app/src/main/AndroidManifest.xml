<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.reborn.reborn">

    <uses-sdk tools:overrideLibrary="com.kakao.sdk:v2-user:2.0.5" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".base.App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Reborn"
        tools:targetApi="31">
        <activity
            android:name=".ui.view.account.AccountActivity"
            >

        </activity>
        <activity android:name=".ui.view.main.MainActivity"/>
        <activity
            android:name=".ui.view.login.LoginActivity">

        </activity>

        <activity
            android:name=".ui.view.assessment.AssessmentActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name="com.reborn.reborn.ui.view.main.search.SearchResultActivity"/>
        <activity android:name=".ui.view.myroutine.MyRoutineActivity"/>
        <activity android:name=".ui.view.main.search.related.RelatedActivity"/>
    </application>

</manifest>